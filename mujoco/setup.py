from setuptools import setup

setup(name='gym_mimic_envs',
      version='0.5',
      # required packages for our env to work
      # uncommented to make ReadTheDocs build work
      # install_requires=['gym', 'mujoco-py']
)

